# HANDOFF

작업 폴더: `C:\Users\gkgk8\my-diary`. 프로젝트 구조와 규칙은 `CLAUDE.md`에 있습니다.
**요청받은 범위 안에서만 작업합니다.**

## 지금까지 한 일

1. **할 일 앱 `index.html`** (1주차): 할 일을 추가, 체크, 삭제하고 완료한 일을 한꺼번에 지울 수 있습니다. 목록은 localStorage `today-todos-v1`에 저장했습니다.
2. **백업**: 다크 모드를 넣기 전 원본을 `index.backup-20260908.html`로 복사해 두었습니다. 이 파일은 수정하지 않습니다.
3. **다크 모드**: 우측 상단 🌙/☀️ 버튼으로 전환합니다. 선택은 localStorage `today-theme`에 저장되어 새로고침해도 유지됩니다.
4. **자기소개 페이지 `about.html`**: 상단 이름, 중간 관심사 카드 3개, 하단 연락처 구조입니다.
   - `index.html`과 링크로 서로 오갈 수 있습니다.
   - 다크 모드 설정을 `index.html`과 같이 씁니다.
   - 이름, 관심사, GitHub, 위치는 아직 임시 내용입니다. 바꿀 곳은 코드 안에 `✏️` 주석으로 표시해 두었습니다.
5. **`CLAUDE.md`**: 프로젝트 안내 파일을 만들고, "지시 범위 밖의 작업은 임의로 하지 않는다"는 규칙을 넣었습니다.
6. **할 일 마감일**: 입력칸 옆에 날짜 선택칸을 붙이고 할 일마다 `due`를 저장했습니다. → **7번에서 소속 날짜(`date`)로 흡수되며 사라졌습니다.**
7. **다이어리로 전환** (2026-09-24): 시중 다이어리 앱(하루콩·Daylio의 달력 중심 구조, 호보니치 테쵸의 먼슬리+데일리 2층 구조)을 참고해 구조를 바꿨습니다.
   - **월 달력이 메인**입니다. `‹ ›`로 달을 옮기고 `오늘` 버튼으로 돌아옵니다. 날짜를 누르면 바로 아래 카드에 그날 상세가 열립니다.
   - **그날 상세**에는 그날의 할 일 목록과 그날의 기록(자유 입력)이 함께 있습니다.
   - **달력 칸 표시는 색에만 기대지 않습니다.** 오늘은 숫자 색+굵기, 선택한 날은 배경+테두리로 서로 다르게 표시합니다. 미완료 할 일은 꽉 찬 점, 완료는 빈 원, 3개를 넘으면 `+N`, 기록이 있으면 `✎`가 붙습니다. 칸마다 `"9월 24일 (목), 할 일 3개 중 2개 남음, 기록 있음"` 형태의 `aria-label`이 있습니다.
   - **키보드**: 달력에서 ←→↑↓로 날짜를 옮기고 달 경계도 넘어갑니다.
   - **저장**: 할 일은 `today-todos-v2`에 `date`와 함께, 기록은 `today-notes-v1`에 `{ '2026-09-24': '...' }` 형태로 날짜별로 저장합니다. 빈 기록은 키째 지웁니다.
   - **마이그레이션**: v2가 없으면 예전 `today-todos-v1`을 읽어 `due`를 `date`로 옮깁니다(마감일이 없던 항목은 오늘로). **`today-todos-v1` 키는 되돌릴 수 있게 일부러 남겨 둡니다.**
   - **기록은 저장 버튼**을 눌러야 저장됩니다. 다만 날짜를 옮기기 직전에 안 쓴 내용이 있으면 이전 날짜에 먼저 저장하고 `앞 날짜 기록을 저장했어요`를 띄웁니다.
   - 새 CSS 토큰을 하나도 추가하지 않아서 `about.html`은 건드리지 않았습니다. 테마 계약이 두 파일에서 여전히 동일한 것을 확인했습니다.
   - **검증**: 로컬 서버 + 헤드리스 크롬으로 자동 테스트 55개를 돌려 전부 통과했습니다(마이그레이션, 점 표시, 날짜 선택, 기록 저장·자동 보관, 달/키보드 이동, XSS, 새로고침 후 유지).
   - 검증 중 버그를 하나 잡았습니다: 달력이 `repeat(7, 1fr)`이라 점 줄의 min-content가 칸 폭을 밀어내 좁은 화면에서 7칸이 카드를 넘쳐 잘렸습니다. `minmax(0, 1fr)`로 고쳤습니다.
8. **깃 저장소 + 공개 배포** (2026-09-24): 홈 폴더에 흩어져 있던 파일을 `my-diary` 폴더로 옮기고 저장소로 만들었습니다.
   - 공개 저장소: https://github.com/Sunwoo-choi-1/my-diary (기본 브랜치 `main`)
   - 홈 폴더에 그대로 `git init`하면 `NTUSER.DAT`·`AppData`·`.claude.json` 같은 개인 파일이 공개로 올라가기 때문에 폴더를 분리했습니다.
   - 커밋 identity는 전역이 아니라 이 저장소에만 지정했고, 공개 커밋에 실제 이메일이 남지 않도록 GitHub noreply 주소를 씁니다.

## 다음에 할 일

우선순위 없이, 다이어리 2단계 후보들입니다.

- **JSON 내보내기/가져오기**: 기기를 옮길 때 쓸 백업. 저장 방식을 "기기 저장 + 내보내기"로 정했을 때 2단계로 미뤄 둔 항목입니다.
- **기분 기록**: 하루콩·Daylio의 핵심 기능. 5단계 이모지를 고르면 달력 칸이 그 색으로 채워져 한 달의 기분 흐름이 보입니다. 넣는다면 달력 칸 디자인을 다시 손봐야 하므로, 칸 표시 규칙(색에만 기대지 않기)을 깨지 않는지 먼저 확인해야 합니다.
- **위클리 뷰**: 몰스킨·호보니치 Cousin의 주간 포맷. 현재는 먼슬리+데일리 2층만 있습니다.
- **월간 요약**: 완료율, 연속 기록일.
- **검색**.

### 결정이 필요한 것

- **마감 알림(마감일 3일 전부터 표시)**: 예전에 이야기가 나왔지만, 7번에서 `due` 필드를 없애고 `date`(소속 날짜) 하나로 합치면서 근거가 되는 값이 사라졌습니다. 이 기능을 하려면 소속 날짜와 별개로 마감일을 다시 둘지부터 정해야 합니다.

### 아직 남아 있는 것

- `about.html`의 이름·관심사·GitHub·위치가 여전히 임시 내용입니다(`✏️` 주석 참고).
- `saveTodos()` / `saveNotes()`는 `writeJson()`을 거쳐 실패 시 `false`를 돌려주지만, 할 일 저장이 실패했을 때는 화면에 알리지 않습니다(기록만 `저장하지 못했어요`를 띄웁니다).
