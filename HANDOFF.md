# HANDOFF

작업 폴더: `C:\Users\gkgk8`. 프로젝트 구조와 규칙은 `CLAUDE.md`에 있습니다.
**요청받은 범위 안에서만 작업합니다.**

## 지금까지 한 일

1. **할 일 앱 `index.html`** (1주차): 할 일을 추가, 체크, 삭제하고 완료한 일을 한꺼번에 지울 수 있습니다. 목록은 localStorage `today-todos-v1`에 저장됩니다.
2. **백업**: 다크 모드를 넣기 전 원본을 `index.backup-20260908.html`로 복사해 두었습니다. 이 파일은 수정하지 않습니다.
3. **다크 모드**: 우측 상단 🌙/☀️ 버튼으로 전환합니다. 선택은 localStorage `today-theme`에 저장되어 새로고침해도 유지됩니다.
4. **자기소개 페이지 `about.html`**: 상단 이름, 중간 관심사 카드 3개, 하단 연락처 구조입니다.
   - `index.html`과 링크로 서로 오갈 수 있습니다.
   - 다크 모드 설정을 `index.html`과 같이 씁니다.
   - 셀프 체크리스트 14개 항목으로 검증했고 모두 통과했습니다.
   - 이름, 관심사, GitHub, 위치는 아직 임시 내용입니다. 바꿀 곳은 코드 안에 `✏️` 주석으로 표시해 두었습니다.
5. **`CLAUDE.md`**: 프로젝트 안내 파일을 만들고, "지시 범위 밖의 작업은 임의로 하지 않는다"는 규칙을 넣었습니다.
6. **할 일 마감일** (이번 작업):
   - 입력칸 옆에 날짜 선택칸(`#todo-due`)을 추가했습니다. 선택 사항이라 비워 둘 수 있습니다.
   - 할 일마다 `due` 값이 `'YYYY-MM-DD'` 형식으로 저장됩니다. 마감일을 비우면 `''`이고, 이 기능 전에 만든 항목에는 `due` 값이 없습니다.
   - 목록에서는 할 일 글자 오른쪽에 `마감 9월 20일 (토)`처럼 보입니다(`.todo-due`). 마감일이 없는 항목은 아무것도 표시하지 않습니다.
   - 날짜 표시는 `formatDue()`가 맡습니다. 시간대 때문에 날짜가 하루 밀리지 않도록 로컬 날짜로 만듭니다.
   - 날짜 선택칸에 다크 모드 색과 모바일(520px 이하) 폭 조정을 적용했습니다.
   - 아직 브라우저에서 직접 확인하지 않았습니다.

## 다음에 할 일

- **마감일 지난 항목 빨간 표시**
  - 오늘 날짜보다 마감일이 이전인 항목을 빨갛게 표시합니다.
  - 관련 위치:
    - `index.html`의 `render()`: 항목에 `.todo-due`를 표시하는 곳
    - 색상: `--danger` 토큰(라이트 `#e36a76`, 다크 `#ef8390`)
  - 결정할 점:
    - 오늘이 마감일인 항목도 지난 것으로 볼지
    - 완료한 항목에도 빨간 표시를 할지
    - 글자만 빨갛게 할지, 줄 전체를 강조할지
  - 참고: 날짜는 `'YYYY-MM-DD'` 문자열이라 오늘 날짜를 같은 형식의 로컬 날짜로 만들면 문자열끼리 크기 비교가 됩니다.
